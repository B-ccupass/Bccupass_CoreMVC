@using Bccupass_CoreMVC.Models.ViewModel.CreateActivity
@model CreateGuestViewModel

<div class="me-4">
    <div class="rounded-circle text-center icon-plus guest-photo" type="button" data-bs-toggle="modal" data-bs-target="#@Model.GuestName">
        <i class="fas fa-plus"></i>
    </div>
    <div class="text-center">
        <p class="mt-3">@Model.GuestName</p>
    </div>
</div>